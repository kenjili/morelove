<?xml version="1.0" encoding="utf-8"?>
<shape android:shape="rectangle"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/holo_red_light" />
    <corners android:topLeftRadius="@dimen/font_size_context"
        android:topRightRadius="@dimen/font_size_context"
        android:bottomLeftRadius="@dimen/font_size_context"
        android:bottomRightRadius="@dimen/font_size_context" />
</shape>